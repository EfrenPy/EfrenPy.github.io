{
  "name": "efrenpy-academic-site",
  "version": "4.0.0",
  "description": "Personal academic website",
  "private": true,
  "scripts": {
    "build:js": "esbuild assets/js/_main.js --bundle --minify --outfile=assets/js/main.min.js",
    "watch:js": "esbuild assets/js/_main.js --bundle --minify --outfile=assets/js/main.min.js --watch",
    "test": "jest --coverage",
    "test:watch": "jest --watch"
  },
  "devDependencies": {
    "@babel/core": "^7.29.0",
    "@babel/preset-env": "^7.29.0",
    "@jest/globals": "^30.2.0",
    "babel-jest": "^30.2.0",
    "esbuild": "^0.25.0",
    "jest": "^30.2.0",
    "jest-environment-jsdom": "^30.2.0",
    "qrcode": "^1.5.4",
    "vcards-js": "^2.10.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
